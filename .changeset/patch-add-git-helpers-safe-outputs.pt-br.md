---
"gh-aw": patch
---

Certifique-se de que `git_helpers.cjs` esteja incluído em `SAFE_OUTPUTS_FILES` para que `generate_git_patch.cjs` e suas dependências sejam carregadas no servidor MCP de saídas seguras.
