---
"gh-aw": patch
---

Adicionar suporte para importação de fluxos de trabalho usando a sintaxe `uses`/`with` com validação `import-schema`, incluindo validação de entradas tipadas e suporte a expressões `github.aw.import-inputs.*` no conteúdo importado.

Descontinuar o uso de `tools.serena` em favor de `mcp-servers.serena` por meio de fluxos de trabalho Serena compartilhados e migrar os fluxos de trabalho agrupados para `shared/mcp/serena.md` e `shared/mcp/serena-go.md`.
