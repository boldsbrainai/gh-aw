---
"gh-aw": patch
---

Documentamos o sinalizador de recurso `features.copilot-requests` para que a autenticação por token do GitHub Actions, as permissões de detecção de ameaças e o ambiente de execução da CLI do Copilot respeitem o fluxo de solicitações do Copilot (injetando permissões `copilot-requests: write` e ativando `S2STOKENS=true`).
