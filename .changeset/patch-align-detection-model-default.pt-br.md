---
"gh-aw": patch
---

Alinhamos o comportamento padrão do modelo de detecção do Copilot com o do agente principal, removendo o recurso de fallback codificado e utilizando o mesmo caminho padrão de string vazia, de modo que o padrão nativo da CLI do Copilot seja aplicado quando nenhum modelo estiver configurado.
