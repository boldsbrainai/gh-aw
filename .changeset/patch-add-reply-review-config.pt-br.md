---
"gh-aw": patch
---

Certifique-se de que a ferramenta de saída segura `reply_to_pull_request_review_comment` seja definida no `config.json` para que o servidor MCP possa habilitar respostas a comentários de revisão e atualize o fluxo de trabalho/fixtures do smoke-copilot para utilizá-la.
