---
"gh-aw": patch
---

Incluir `github.event.label.name` no grupo de concorrência para fluxos de trabalho acionados por rótulo (sintaxe abreviada de acionamento por rótulo e `label_command`). Isso evita o cancelamento entre rótulos quando vários rótulos são adicionados simultaneamente ao mesmo PR ou issue.
