---
"gh-aw": menor
---
Renomeamos a integração do fluxo de trabalho SafeInputs para MCP Scripts (campo de frontmatter `SafeInputs`/`safe-inputs`, referências no GitHub, documentação, ferramentas e logs) e adicionamos o codemod `safe-inputs-to-mcp-scripts` para que os fluxos de trabalho existentes possam ser migrados automaticamente.
